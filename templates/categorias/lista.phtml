<?php require 'templates/layout/header.phtml' ?>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <h1 class="mb-4">Categorías</h1>
                <p>Filtrar por categoría:</p>
                <?php foreach ($generos as $genero): ?>
                    <button class="btn btn-outline-primary mb-2" 
                            onclick="window.location.href='<?= BASE_URL ?>librosPorCategoria/<?= urlencode($genero['genero']) ?>'">
                        <?= htmlspecialchars($genero['genero']) ?>
                    </button>
                <?php endforeach; ?>
            </div>

            <div class="col-md-6">
                <h2 class="mb-4">Editar o eliminar categorías</h2>
                <p>Seleccione el género que quiera editar o eliminar:</p>
                <ul class="list-group">
                    <?php foreach ($generos as $genero): ?>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <?= htmlspecialchars($genero['genero']) ?>

                        <div>
                            <!-- Botón para editar -->
                            <button class="btn btn-warning btn-sm me-2"
                                onclick="window.location.href='<?= BASE_URL ?>editarCategoria/<?= urlencode($genero['genero']) ?>'">
                                Editar
                            </button>

                            <!-- Botón para eliminar -->
                            <button class="btn btn-danger btn-sm"
                                onclick="if(confirm('¿Estás seguro de que deseas eliminar esta categoría?')) window.location.href='<?= BASE_URL ?>eliminarCategoria/<?= urlencode($genero['genero']) ?>'">
                                Eliminar
                            </button>
                        </div>
                    </li>
                    <?php endforeach; ?>
                </ul>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous">
    </script>
</body>

</html>
